# 2026-09-23

Slots: all three busy. challenger1 (H1, day 7.7 of 60), challenger2 (H2, day
2.6 of 60), challenger3 (H3, day 2.0 of 60). No verdicts yet (LEDGER.md has no
`## Results` sections), so nothing for FINDINGS.md to pick up today.
`gh pr list --state closed --limit 5` shows nothing new since notes/2026-09-22.md
(#9 merged); #5 is the same old backtest_gate slot-1 fallback closure already
covered.

## Decisions.csv review

Read the last ~130 rows of state/champion/decisions.csv and challenger3's
(both halted today), plus the last ~70 of challenger1 and challenger2. No
contradictions between stated reason and action anywhere.

**The DOT/AVAX cash-starvation pattern flagged 2026-09-22 is still live and
got worse.** champion's DOT sat frozen at weight 0.026 against a target near
0.10-0.11 for another full day (same `none: no fill possible (no cash or no
position)` reason, ts 1790155367 through 1790162534), and AVAX kept firing
`enter long` with target 0.09-0.24 and getting priced out the same way,
right up until champion's own daily halt fired at 1790173423 (equity down
5.1% from the day's open) and flattened everything for the rest of the UTC
day. DOT did finally exit once (ts 1790166441, "exit: price more than 2.0%
below 24h EMA") just before the halt. challenger3 shows the same shape on
the same pairs and also halted at the same equity-drawdown threshold.
This is the same root cause already written up for Fin in notes/2026-09-22.md
(apply_limits scales signal weights to a gross cap without regard to
realisable cash, and the fixed pairs-order execution loop lets early-settled
pairs sit still instead of trimming to fund laggards) — protected code, so
no fix goes in this PR. Not re-explaining the mechanism again here; flagging
only that it is still active and that today's halt likely means DOT and AVAX
have now been meaningfully underweight across two account-days in a row,
which is worth knowing if either account's prospective numbers look odd
against what ts_momentum/swing_reversal actually signalled.

challenger1 (mean_reversion) sat flat everywhere — every pair's z-score has
been running +0.1 to +1.3 for the last several hours, nowhere near the -2.0
entry trigger. Expected behaviour for a bounce-buyer strategy in a market
that has been drifting up, not down. challenger2 (vol_breakout) also sat
flat everywhere, still "168h vol not at or below its own 25% percentile" on
every pair — consistent with the already-documented ramp-up issue
(FINDINGS.md: vol_breakout needs ~1610 live candles before it can fire at
all, not expected to clear that until roughly 2026-10-22).

No missing hours in the Data section for any pair. No bug in bot/strategy.py
found.

## Backlog

No changes. Nothing above is a new hypothesis; it's confirmation that
yesterday's flagged execution-mechanics issue is ongoing, for Fin.
