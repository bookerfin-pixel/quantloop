# 2026-09-26

Slots: all four busy. challenger1 (H1, day 10.7 of 60), challenger2 (H2, day
5.6 of 60), challenger3 (H3, day 1.8 of 60), challenger4 (H4, day 1.0 of 60).
No verdicts yet (LEDGER.md has no `## Results` sections), so nothing for
FINDINGS.md to pick up today. `gh pr list --state closed --limit 5` shows
five MERGED PRs (#8-#12), nothing closed by the gate to read comments on.

## Decisions.csv review

Read the last ~24h of rows (ts >= 1790371313) for champion and challenger4
(both new/still building history), and spot-checked the tails of
challenger1, challenger2 and challenger3. No contradictions between stated
reason and action anywhere.

**The cash-starvation pattern flagged 2026-09-22 through 2026-09-24 (DOT/AVAX)
has recurred today, now on ADA, ETH and XRP instead.** champion's ADA sat at
`none: no fill possible (no cash or no position)` for three straight hours
(ts 1790425538-1790432499) while its target held near 0.20 and current
weight drifted at 0.145, then ETH and XRP hit the same reason once each (ts
1790439758) when AVAX and DOGE entries used up the freed cash first. This is
the same root cause already written up for Fin in notes/2026-09-22.md
(apply_limits scales signal weights to a gross cap without regard to
realisable cash, and the fixed pairs-order execution loop lets whichever pair
sorts first claim freed cash before a later one in the list) — protected
code (bot/risk.py / bot/paper.py / bot/run.py), so no fix goes in this PR.
Worth flagging only because it is not confined to DOT/AVAX: any pair can get
starved depending on alphabetical order and which positions free up cash
first in a given hour, so a challenger's own book composition determines
which of its pairs eats the friction, not something about DOT or AVAX
specifically.

challenger4 (ts_momentum, slow) built its book correctly on its first hour
(the 2026-09-25 fix: "first hour under a new strategy, decided as if flat")
and has held ten small positions all day with only vol-target-driven weight
drift since, no cash starvation observed (it never fully saturates a gross
cap the way champion's tighter bands do). challenger1 (mean_reversion) sat
flat everywhere all day, z-scores +0.5 to +2.2, nowhere near the -2.0 entry
trigger — expected in a continuing rally. challenger2 (vol_breakout) and
challenger3 (swing_reversal) also sat flat everywhere today; no fills, no
`none` (cash-blocked) rows for either, consistent with both still waiting on
their entry preconditions (documented ramp-up for H2, no qualifying higher
low for H3).

No missing hours in the Data section for any pair. No bug in bot/strategy.py
found.

## Backlog

No changes. Nothing above is a new hypothesis; it's confirmation that the
cash-starvation execution-mechanics issue already flagged for Fin is
ongoing and not pair-specific.
