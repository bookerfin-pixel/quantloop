# 2026-09-16

Slot busy: H1 (mean_reversion, 240h window) testing since 2026-09-16 05:21Z,
day 0.7 of 21. No PRs closed by the gate recently (only the H1 merge).

Reviewed the full day's decisions.csv for both accounts (114 rows each, since
account start at 03:55Z) and the Data section of the summary.

- Champion (ts_momentum): every row is flat, reason is consistently "72h
  return not above entry band +1.0%" (or below it and below EMA). Matches
  the observed 72h returns in the log, which have stayed negative to flat
  all day. No contradiction between reason and action, no stuck-at-zero
  weight without cause — it's flat because the entry condition genuinely
  isn't met.
- Challenger (mean_reversion): BTC, ETH, SOL, ADA all entered longs when
  their z-score crossed below -2.0, sizes matched the vol-target formula
  (e.g. BTC vol 33% -> weight 0.25, ETH vol 58%/59% -> weight 0.25), and
  weights drift only slightly between runs from price moves, consistent
  with "hold, change below threshold". AVAX and LINK haven't traded yet;
  their z-scores have stayed above -2.0 all day (closest was LINK at
  -1.79), which the log reflects each time — not a bug, just no signal yet
  on those two pairs.
- Data section: 0 missing hours in the last 7d for all six pairs.

No bug found. Nothing new for the backlog today; the existing candidates
still stand as written. Kept this run short per the busy-slot procedure.
